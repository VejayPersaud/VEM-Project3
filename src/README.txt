#To use main.cpp open the file in your IDE of choice and start debugging
#We recommend Visual Studio
#A debugging window should pop up asking for input
#Put "t" to run the sorting lists and it will output 15 groups of information
#Each group has the runtime of a normal sort and a multithreaded version of the sort
